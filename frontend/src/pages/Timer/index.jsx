import React from "react";
import Layout from "../../layout";
import Timerlayout from "./Timer";
const Timer = () => {
  return (
    <Layout
      pages={Timerlayout()}
    ></Layout>
  );
};

export default Timer;
